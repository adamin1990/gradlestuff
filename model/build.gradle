apply plugin: 'java'

dependencies {
    def modelDependencies  = rootProject.ext.modelDependencies
    compile   modelDependencies.rxJava
    compile   modelDependencies.fireBase
    compile fileTree(dir: 'libs', include: ['*.jar'])
}